{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write",
        "hooks": [
          {
            "type": "command",
            "command": "f=$(jq -r '.tool_input.file_path // empty'); if [[ \"$f\" =~ /(01_python_basics|02_oop|03_patterns)/[^/]+\\.py$ ]] && [[ \"$f\" != *_practice.py ]]; then python3 \"$f\" >/dev/null 2>&1 || printf '{\"systemMessage\": \"⚠️ Python verification failed for: %s\"}\\n' \"$f\"; fi",
            "statusMessage": "Verifying Python reference file..."
          }
        ]
      }
    ]
  }
}
